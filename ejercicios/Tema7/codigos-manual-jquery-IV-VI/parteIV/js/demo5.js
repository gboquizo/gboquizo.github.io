/**
 * Demo 5, parte IV
 * @author Guillermo Boquizo Sánchez
 */
{
    let init = function () {
        $("#boton").click(function (i) {
            $("td").removeAttr("noWrap");
        });

    }
    $(init);
}