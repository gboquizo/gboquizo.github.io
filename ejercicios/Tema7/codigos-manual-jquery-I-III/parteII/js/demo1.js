/**
 * Demo 1, parte II
 * @author Guillermo Boquizo Sánchez
 */
{
    let init = function () {
        let paragraph = $("p");

        alert("Hay " + paragraph.length + " párrafos en la página");
    }
    $(init)
}